import React from 'react'
import useStyles from './styles'

const LeftSidebar = () => {
    const classes = useStyles()
    return (
        <div className={classes.root}>leftSidebar</div>

    );
};

export default LeftSidebar;